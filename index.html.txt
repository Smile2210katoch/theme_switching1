<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard</title>
  <style>
    :root {
      --header-bg: #4caf50;
      --sidebar-bg: #f4f4f4;
      --main-bg: #ffffff;
      --footer-bg: #4caf50;
      --text-color: #000000;
      --link-color: purple;
    }

    body.dark {
      --header-bg: #333;
      --sidebar-bg: #2b2b2b;
      --main-bg: #1e1e1e;
      --footer-bg: #222;
      --text-color: #eeeeee;
      --link-color: #9999ff;
    }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: var(--main-bg);
      color: var(--text-color);
      transition: background-color 0.3s, color 0.3s;
    }

    .dashboard {
      display: grid;
      grid-template-areas:
        "header header"
        "sidebar main"
        "footer footer";
      grid-template-columns: 200px 1fr;
      grid-template-rows: auto 1fr auto;
      height: 100vh;
      border: 1px solid #000;
    }

    .header {
      grid-area: header;
      background-color: var(--header-bg);
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 20px;
    }

    .theme-switch {
      color: white;
    }

    .sidebar {
      grid-area: sidebar;
      background-color: var(--sidebar-bg);
      padding: 20px;
    }

    .sidebar ul {
      list-style-type: disc;
      padding-left: 20px;
    }

    .sidebar a {
      text-decoration: underline;
      color: var(--link-color);
      cursor: pointer;
    }

    .main-content {
      grid-area: main;
      padding: 30px;
      background-color: var(--main-bg);
    }

    .footer {
      grid-area: footer;
      text-align: center;
      padding: 10px;
      background-color: var(--footer-bg);
      color: white;
    }

    /* Modal styles */
    .modal {
      display: none;
      position: fixed;
      z-index: 999;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0,0,0,0.5);
    }

    .modal-content {
      background-color: #fff;
      color: #000;
      margin: 15% auto;
      padding: 20px;
      width: 300px;
      border-radius: 8px;
      text-align: center;
      position: relative;
    }

    .dark .modal-content {
      background-color: #333;
      color: #eee;
    }

    .close-btn {
      position: absolute;
      top: 5px;
      right: 10px;
      font-size: 20px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="dashboard">
    <header class="header">
      <h1>Admin Dashboard</h1>
      <label class="theme-switch" for="theme-toggle">
        <input type="checkbox" id="theme-toggle" />
        Dark Mode
      </label>
    </header>

    <aside class="sidebar">
      <ul>
        <li><a onclick="showModal('Home')">Home</a></li>
        <li><a onclick="showModal('Users')">Users</a></li>
        <li><a onclick="showModal('Settings')">Settings</a></li>
      </ul>
    </aside>

    <main class="main-content">
      <h2>Welcome, Admin!</h2>
      <p>This is the main content area. Add charts, tables, or reports here.</p>
    </main>

    <footer class="footer">
      © 2025 Admin Panel
    </footer>
  </div>

  <!-- Popup Modal -->
  <div id="popup-modal" class="modal">
    <div class="modal-content">
      <span class="close-btn" onclick="closeModal()">&times;</span>
      <h3 id="modal-title">Modal Title</h3>
      <p id="modal-message">This is a popup message.</p>
    </div>
  </div>

  <script>
    const toggle = document.getElementById('theme-toggle');
    toggle.addEventListener('change', () => {
      document.body.classList.toggle('dark');
    });

    function showModal(section) {
      document.getElementById('modal-title').innerText = section + ' Section';
      document.getElementById('modal-message').innerText = `You clicked on "${section}". More features can be added here.`;
      document.getElementById('popup-modal').style.display = 'block';
    }

    function closeModal() {
      document.getElementById('popup-modal').style.display = 'none';
    }

    // Optional: close modal if user clicks outside it
    window.onclick = function(event) {
      const modal = document.getElementById('popup-modal');
      if (event.target == modal) {
        modal.style.display = "none";
      }
    };
  </script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard</title>
  <style>
    :root {
      --header-bg: #4caf50;
      --sidebar-bg: #f4f4f4;
      --main-bg: #ffffff;
      --footer-bg: #4caf50;
      --text-color: #000000;
      --link-color: purple;
    }

    body.dark {
      --header-bg: #333;
      --sidebar-bg: #2b2b2b;
      --main-bg: #1e1e1e;
      --footer-bg: #222;
      --text-color: #eeeeee;
      --link-color: #9999ff;
    }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: var(--main-bg);
      color: var(--text-color);
      transition: background-color 0.3s, color 0.3s;
    }

    .dashboard {
      display: grid;
      grid-template-areas:
        "header header"
        "sidebar main"
        "footer footer";
      grid-template-columns: 200px 1fr;
      grid-template-rows: auto 1fr auto;
      height: 100vh;
      border: 1px solid #000;
    }

    .header {
      grid-area: header;
      background-color: var(--header-bg);
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 20px;
    }

    .theme-switch {
      color: white;
    }

    .sidebar {
      grid-area: sidebar;
      background-color: var(--sidebar-bg);
      padding: 20px;
    }

    .sidebar ul {
      list-style-type: disc;
      padding-left: 20px;
    }

    .sidebar a {
      text-decoration: underline;
      color: var(--link-color);
      cursor: pointer;
    }

    .main-content {
      grid-area: main;
      padding: 30px;
      background-color: var(--main-bg);
    }

    .footer {
      grid-area: footer;
      text-align: center;
      padding: 10px;
      background-color: var(--footer-bg);
      color: white;
    }

    /* Modal styles */
    .modal {
      display: none;
      position: fixed;
      z-index: 999;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0,0,0,0.5);
    }

    .modal-content {
      background-color: #fff;
      color: #000;
      margin: 15% auto;
      padding: 20px;
      width: 300px;
      border-radius: 8px;
      text-align: center;
      position: relative;
    }

    .dark .modal-content {
      background-color: #333;
      color: #eee;
    }

    .close-btn {
      position: absolute;
      top: 5px;
      right: 10px;
      font-size: 20px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="dashboard">
    <header class="header">
      <h1>Admin Dashboard</h1>
      <label class="theme-switch" for="theme-toggle">
        <input type="checkbox" id="theme-toggle" />
        Dark Mode
      </label>
    </header>

    <aside class="sidebar">
      <ul>
        <li><a onclick="showModal('Home')">Home</a></li>
        <li><a onclick="showModal('Users')">Users</a></li>
        <li><a onclick="showModal('Settings')">Settings</a></li>
      </ul>
    </aside>

    <main class="main-content">
      <h2>Welcome, Admin!</h2>
      <p>This is the main content area. Add charts, tables, or reports here.</p>
    </main>

    <footer class="footer">
      © 2025 Admin Panel
    </footer>
  </div>

  <!-- Popup Modal -->
  <div id="popup-modal" class="modal">
    <div class="modal-content">
      <span class="close-btn" onclick="closeModal()">&times;</span>
      <h3 id="modal-title">Modal Title</h3>
      <p id="modal-message">This is a popup message.</p>
    </div>
  </div>

  <script>
    const toggle = document.getElementById('theme-toggle');
    toggle.addEventListener('change', () => {
      document.body.classList.toggle('dark');
    });

    function showModal(section) {
      document.getElementById('modal-title').innerText = section + ' Section';
      document.getElementById('modal-message').innerText = `You clicked on "${section}". More features can be added here.`;
      document.getElementById('popup-modal').style.display = 'block';
    }

    function closeModal() {
      document.getElementById('popup-modal').style.display = 'none';
    }

    // Optional: close modal if user clicks outside it
    window.onclick = function(event) {
      const modal = document.getElementById('popup-modal');
      if (event.target == modal) {
        modal.style.display = "none";
      }
    };
  </script>
</body>
</html>

document.addEventListener("DOMContentLoaded", function() {
    var readDocs = document.querySelector(".read-instructions");
    readDocs.style.display = "block";
});